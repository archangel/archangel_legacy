<nav class="navbar navbar-expand navbar-dark bg-dark static-top">
  <a class="navbar-brand" href="<%= backend_root_path %>">
    <%= image_tag(current_site.logo.tiny.url, alt: current_site.name, class: "d-inline-block align-top") %>

    <%= current_site.name %>
  </a>

  <div id="navbarTop" class="navbar-collapse collapse">
    <ul class="navbar-nav ml-auto">
      <li class="nav-item">
        <a class="nav-link" href="<%= root_path %>" taeget="_blank"><%= Archangel.t(:website) %></a>
      </li>

      <li class="nav-item dropdown no-arrow">
        <a class="nav-link dropdown-toggle" href="#" id="userDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
          <%= image_tag(current_user.avatar.tiny.url, alt: current_user.username, class: "rounded") %>
        </a>

        <div class="dropdown-menu dropdown-menu-right" aria-labelledby="userDropdown">
          <a class="dropdown-item" href="<%= backend_profile_path %>"><%= Archangel.t(:profile) %></a>

          <div class="dropdown-divider" role="separator"></div>

          <a class="dropdown-item" href="<%= destroy_user_session_path %>" data-method="delete" rel="nofollow"><%= Archangel.t(:logout) %></a>
        </div>
      </li>
    </ul>
  </div>
</nav>
