<% unless flash.empty? %>
  <div id="alert-messages">
    <% flash.delete(:timedout).each do |key, value| %>
      <div class="alert alert-<%= flash_class_for(key) %> alert-link alert-dismissible" role="alert">
        <%= value %>

        <button type="button" class="close" data-dismiss="alert" aria-label="<%= Archangel.t(:close) %>">
          <span aria-hidden="true">×</span>
        </button>
      </div>
    <% end %>
  </div>
<% end %>
