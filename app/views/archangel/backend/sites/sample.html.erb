<div class="card card-default">
  <div class="card-body">
    <h2><%= fa_icon("cog") %> <%= Archangel.t(:show_resource, resource: @site.model_name.human.titleize) %></h2>
  </div>
</div>

<div class="container-fluid">
  <div class="row">
    <div class="col-lg-12">
      <%= render "colors" %>
      <%= render "grays" %>
      <%= render "additional_colors" %>

      <%= render "headings" %>
      <%= render "display_headings" %>
      <%= render "inline_text" %>
      <%= render "dl" %>

      <%= render "cards" %>
      <%= render "carousel" %>
      <%= render "forms" %>
      <%= render "jumbotron" %>
      <%= render "list_group" %>
      <%= render "navs" %>
      <%= render "pagination" %>
      <%= render "tables" %>

      <%= render "button" %>
      <%= render "buttons_group" %>
      <%= render "dropdowns" %>
      <%= render "social_buttons" %>

      <%= render "alerts" %>
      <%= render "badge" %>
      <%= render "modals" %>
    </div>
  </div>
</div>
