<div class="card card-default sticky-title rounded-0">
  <div class="card-body">
    <h2><%= Archangel.t(:new_resource, resource: @collection.model_name.human.titleize) %></h2>
  </div>
</div>

<div class="card collections-new">
  <div class="card-header">
    <%= render "buttons" %>
  </div>

  <div class="card-body">
    <%= render "form", collection: @collection %>
  </div>
</div>
