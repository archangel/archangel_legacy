<div class="card card-default">
  <div class="card-body">
    <h2><%= Archangel.t(:new_resource, resource: @asset.model_name.human.titleize) %></h2>

    <%= render "buttons" %>
  </div>
</div>

<div class="assets-new card card-default">
  <div class="card-body">
    <%= render "form", asset: @asset %>
  </div>
</div>
