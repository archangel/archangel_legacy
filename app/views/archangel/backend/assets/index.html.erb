<div class="panel panel-default">
  <div class="panel-body">
    <h2><%= Archangel.t(:index_resource, resource: @assets.model_name.human.titleize) %></h2>

    <%= render "buttons" %>
  </div>
</div>

<div class="assets-index panel panel-default">
  <div class="panel-body">
    <% if @assets.present? %>

      <%= paginate @assets %>

      <div class="table-responsive">
        <table class="table table-hover table-bordered table-striped">
          <thead>
            <tr>
              <th scope="col"><%= Archangel.t(:file_name) %></th>
              <th scope="col"><%= Archangel.t(:file) %></th>
              <th scope="col"><%= Archangel.t(:content_type) %></th>
              <th scope="col"><%= Archangel.t(:file_size) %></th>
              <th class="actions text-right" scope="col"><%= Archangel.t(:actions) %></th>
            </tr>
          </thead>

          <tbody>
            <% @assets.each do |asset| %>
              <tr>
                <td><%= asset.file_name %></td>
                <td><%= asset.file %></td>
                <td><%= asset.content_type %></td>
                <td><%= number_to_human_size(asset.file_size) %></td>
                <td class="actions text-right">
                  <%= link_to(glyphicon_icon("info-sign", text: Archangel.t(:show)), backend_asset_path(asset), class: "btn btn-info btn-sm") %>
                  <%= link_to(glyphicon_icon("ok-sign", text: Archangel.t(:edit)), edit_backend_asset_path(asset), class: "btn btn-warning btn-sm") %>
                  <%= link_to(glyphicon_icon("remove-sign", text: Archangel.t(:destroy)), backend_asset_path(asset), class: "btn btn-danger btn-sm",
                                                            method: :delete, data: { confirm: Archangel.t(:are_you_sure) }) %>
                </td>
              </tr>
            <% end %>
          </tbody>
        </table>
      </div>

      <%= paginate @assets %>

    <% else %>

      <p class="no-resources">
        <%= Archangel.t(:no_resources, resource: @assets.model_name.human.pluralize.downcase) %>
      </p>

    <% end %>
  </div>
</div>
